import React, {useEffect} from 'react';

// we will re render after every send event so that new messages can be displayed
const index = () => {
  return (
    <div>index</div>
  )
}

export default index